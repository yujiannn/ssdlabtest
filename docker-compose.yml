version: '3.8'

services:
  jenkins:
    build: ./jenkins-docker
    ports:
      - "8080:8080"
    volumes:
      - jenkins_home:/var/jenkins_home

  nginx:
    image: nginx:latest
    ports:
      - "80:80"

volumes:
  jenkins_home:
